# Core ML and NLP libraries (PyTorch required by spec for transformers models)
# Keep MPS support for Apple Silicon M1 acceleration
torch>=2.4.0,<2.9
transformers>=4.44.0
spacy>=3.7.0
langdetect>=1.0.9

# Data processing - optimized choices
pandas>=2.2.0
numpy>=1.26.0
fastparquet>=2024.5.0  # Lighter than pyarrow (112MB → ~5MB)
scikit-learn>=1.5.0    # Core ML algorithms

# Topic modeling - full unsupervised discovery stack
sentence-transformers>=3.0.0  # For embeddings
bertopic>=0.16.0              # Unsupervised topic modeling
hdbscan>=0.8.33               # Clustering algorithm
umap-learn>=0.5.5             # Dimensionality reduction

# Configuration and utilities
pyyaml>=6.0
click>=8.1.0
tqdm>=4.66.0
python-dotenv>=1.0.0
psutil>=5.8.0  # Memory monitoring for checkpoints

# CLI UI
textual>=0.48.0,<1.0.0

# URL/domain processing
tldextract>=3.6.0

# DEBLOATING NOTES:
# Reduced dependencies while maintaining functionality:
# - pyarrow (112MB) → fastparquet (~5MB) 
# - Development tools moved to requirements-dev.in
# - plotly (65MB) moved to dev (visualization)
# - BERTopic stack restored for M5 topic discovery requirements
